import { Text, View } from 'react-native';

const SplashScreen = () => {
  return (
    <View className="flex-1 bg-figmaBlue items-center justify-center">
      <Text className="text-[42px] font-extrabold tracking-wide text-figmaPink">BOSSITIVE</Text>
    </View>
  );
};

export default SplashScreen;
